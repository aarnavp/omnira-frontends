/** Device categories the Contribute side of the network runs on — mirrors
 * the brand screenshot's own breakdown (Computers / Phones / TVs & streamers)
 * plus Cloud as the non-device failover row shown alongside them. */
export type DeviceKind = "computer" | "phone" | "tv" | "iot";
